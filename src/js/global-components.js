import heroDrop from '../global-components/hero-drop/heroDrop';
import faqAccordion from '../global-components/blocco-faq/faqAccordion';

const globalComponents = () => {
	heroDrop();
	faqAccordion();
};

export default globalComponents;
